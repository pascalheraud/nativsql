# NativSQL configuration for tests
nativsql.enum-mapping.scan-packages=ovh.heraud.nativsql.domain

# Disable embedded database auto-configuration
spring.test.database.replace=none
spring.sql.init.mode=never
